# ── Server ──────────────────────────────────────────────────────
PORT=3000
NODE_ENV=development

# ── Security ─────────────────────────────────────────────────────
# Genera un valor seguro con:  node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=cambia_esto_por_una_cadena_aleatoria_larga_en_produccion

# ── Base de datos ─────────────────────────────────────────────────
# Dejar vacío usa ./db/mendez.db por defecto
DB_PATH=

# ── Email SMTP ────────────────────────────────────────────────────
# Dejar vacío = modo consola (imprime códigos en terminal, útil en dev)
#
# Mailtrap (testing):
#   SMTP_HOST=sandbox.smtp.mailtrap.io  SMTP_PORT=2525
# Gmail:
#   SMTP_HOST=smtp.gmail.com  SMTP_PORT=587  SMTP_SECURE=false
# SendGrid:
#   SMTP_HOST=smtp.sendgrid.net  SMTP_PORT=587  SMTP_USER=apikey
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@mendeztransport.com

# ── Demo credentials — Patient (override in Netlify env vars) ────
DEMO_EMAIL=demo@mendeztransport.com
DEMO_PASS=Mendez2026!
DEMO_CODE=123456
DEMO_TRIP=MT-2026-4891
DEMO_CONF=7823

# ── Demo credentials — Dispatcher ────────────────────────────────
DISPATCHER_EMAIL=dispatcher@mendeztransport.com
DISPATCHER_PASS=Dispatch2026!
DISPATCHER_CODE=654321

# ── Demo credentials — Driver (Carlos Rivera) ─────────────────────
DRIVER_EMAIL=driver@mendeztransport.com
DRIVER_PASS=Driver2026!
DRIVER_CODE=789012

# ── Broker portal access code ─────────────────────────────────────
BROKER_KEY=Broker2026!
